<UserControl x:Class="Gnode.UI.WPF.NodeEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Gnode.UI.WPF"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.ContextMenu>
        <ContextMenu>
            <MenuItem Header="Create Node" Click="CreateNode_Click" />
            <MenuItem Header="Delete Node" Click="DeleteNode_Click" />
            <MenuItem Header="Create Connection" Click="CreateConnection_Click" />
            <MenuItem Header="Delete Connection" Click="DeleteConnection_Click" />
        </ContextMenu>
    </UserControl.ContextMenu>
    <ScrollViewer>
        <local:GraphControl x:Name="graphControl" />
    </ScrollViewer>
</UserControl>
